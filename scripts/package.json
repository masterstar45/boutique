{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "db-backup": "tsx ./src/db-backup.ts",
    "db-restore": "tsx ./src/db-restore.ts",
    "security-check": "tsx ./src/security-check.ts",
    "smoke": "tsx ./src/post-deploy-smoke.ts",
    "ops-check": "pnpm run security-check ; pnpm run smoke",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.18.0",
    "tsx": "catalog:"
  }
}
